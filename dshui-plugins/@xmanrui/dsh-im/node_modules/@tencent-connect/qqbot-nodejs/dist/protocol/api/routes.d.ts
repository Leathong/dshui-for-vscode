/**
 * Centralized API route templates for the QQ Open Platform.
 *
 * Eliminates C2C/Group path duplication by parameterizing on `ChatScope`.
 */
import type { ChatScope } from "../types.js";
export declare function messagePath(scope: ChatScope, targetId: string): string;
export declare function channelMessagePath(channelId: string): string;
export declare function dmMessagePath(guildId: string): string;
export declare function mediaUploadPath(scope: ChatScope, targetId: string): string;
export declare function uploadPreparePath(scope: ChatScope, targetId: string): string;
export declare function uploadPartFinishPath(scope: ChatScope, targetId: string): string;
export declare function uploadCompletePath(scope: ChatScope, targetId: string): string;
export declare function streamMessagePath(openid: string): string;
export declare function gatewayPath(): string;
export declare function interactionPath(interactionId: string): string;
/**
 * Generate a message sequence number in the 0..65535 range.
 *
 * Used by both `messages.ts` and `media.ts` to avoid duplicate definitions.
 */
export declare function getNextMsgSeq(_msgId: string): number;
//# sourceMappingURL=routes.d.ts.map