/**
 * Message-sending API for the QQ Open Platform.
 */
import type { ChatScope, Credentials, InlineKeyboard, Logger, MessageResponse, OutboundMeta, StreamMessageRequest } from "../types.js";
import { ApiClient } from "./api-client.js";
import { TokenManager } from "./token.js";
export interface MessageApiConfig {
    /** Whether the QQ Bot has markdown permission. */
    markdownSupport: boolean;
    logger?: Logger;
}
type OnMessageSentCallback = (refIdx: string, meta: OutboundMeta) => void;
export declare class MessageApi {
    private readonly client;
    private readonly tokenManager;
    private readonly markdownSupport;
    private readonly logger?;
    private messageSentHook;
    constructor(client: ApiClient, tokenManager: TokenManager, config: MessageApiConfig);
    onMessageSent(callback: OnMessageSentCallback): void;
    notifyMessageSent(refIdx: string, meta: OutboundMeta): void;
    sendMessage(scope: ChatScope, targetId: string, content: string, creds: Credentials, opts?: {
        msgId?: string;
        messageReference?: string;
        inlineKeyboard?: InlineKeyboard;
    }): Promise<MessageResponse>;
    sendProactiveMessage(scope: ChatScope, targetId: string, content: string, creds: Credentials): Promise<MessageResponse>;
    sendChannelMessage(opts: {
        channelId: string;
        content: string;
        creds: Credentials;
        msgId?: string;
    }): Promise<MessageResponse>;
    sendDmMessage(opts: {
        guildId: string;
        content: string;
        creds: Credentials;
        msgId?: string;
    }): Promise<MessageResponse>;
    /** Send a typing indicator to a C2C user. */
    sendInputNotify(opts: {
        openid: string;
        creds: Credentials;
        msgId?: string;
        inputSecond?: number;
    }): Promise<{
        refIdx?: string;
    }>;
    acknowledgeInteraction(interactionId: string, creds: Credentials, code?: number, data?: Record<string, unknown>): Promise<void>;
    /** Get the WebSocket gateway URL for the bot. */
    getGatewayUrl(creds: Credentials): Promise<string>;
    /**
     * Send a C2C stream message chunk (`/v2/users/{openid}/stream_messages`).
     * Only supported for one-to-one chats.
     */
    sendC2CStreamMessage(creds: Credentials, openid: string, req: StreamMessageRequest): Promise<MessageResponse>;
    /**
     * Raw message send — transparently forwards all fields to the QQ Open Platform API.
     *
     * This is the "escape hatch" for any message type not covered by the
     * higher-level helpers. Fields like `msg_type`, `markdown`, `ark`, `embed`,
     * `keyboard`, `media`, `message_reference`, `is_wakeup` etc. are passed through
     * as-is to `/v2/users/{openid}/messages` or `/v2/groups/{group_openid}/messages`.
     *
     * Auto-injects `msg_seq` if not provided.
     */
    sendRaw(scope: ChatScope, targetId: string, creds: Credentials, body: Record<string, unknown>): Promise<MessageResponse>;
    /**
     * Send a message to a guild text channel.
     * Supports content, keyboard, message_reference, and arbitrary extra fields.
     */
    sendChannelMessageRaw(channelId: string, creds: Credentials, body: Record<string, unknown>): Promise<MessageResponse>;
    /**
     * Send a DM (direct message) in a guild.
     */
    sendDmMessageRaw(guildId: string, creds: Credentials, body: Record<string, unknown>): Promise<MessageResponse>;
    /**
     * Recall (delete) a message.
     */
    recallMessage(scope: ChatScope, targetId: string, messageId: string, creds: Credentials): Promise<void>;
    private sendAndNotify;
    private buildMessageBody;
    private buildProactiveBody;
}
export { getNextMsgSeq } from "./routes.js";
//# sourceMappingURL=messages.d.ts.map