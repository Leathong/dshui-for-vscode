import type { ReactElement } from 'react';
/** Full 24×24 glyph, rendered at 14px — sidebar dock undo buttons. */
export declare function IconUndoOutline14({ size, className }: {
    size?: number;
    className?: string;
}): ReactElement;
/** Cropped glyph core (viewBox 4 4 16 16), rendered at 16px — message rollback action button. */
export declare function IconUndoOutline16({ size, className }: {
    size?: number;
    className?: string;
}): ReactElement;
